# Pinned to versions verified working together on 2026-08-06.
# Requires Python 3.11+.

# Data handling
pandas==2.3.3
openpyxl==3.1.5          # reads the DOL .xlsx disclosure files
pyarrow==24.0.0          # Parquet cache; raw .xlsx are far too slow to re-read

# Dashboard
streamlit==1.61.1
plotly==6.9.0

# Azure (Phase 2 only). Nothing in the Phase 1 pipeline or app.py imports
# these; src/etl/ is the only consumer, and tests/test_blob.py skips itself if
# they are absent. Entra auth only — allowSharedKeyAccess is false on the
# storage account, so there is no account key for a connection string to carry.
azure-storage-blob==12.30.0
azure-identity==1.25.3
# Needs the Microsoft ODBC Driver 18 present on the machine — pip cannot
# supply it. macOS: brew tap microsoft/mssql-release && brew trust
# microsoft/mssql-release && HOMEBREW_ACCEPT_EULA=Y brew install msodbcsql18
pyodbc==5.3.0

# Testing
pytest==9.1.1
